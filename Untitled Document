def is_list_permutation(L1, L2):
  L1.sort()
  L2.sort()
  t=()
  if L1!=L2:
    return False
  else:
    maxone=max(set(L1), key=L1.count) 
    t = t + (maxone,L1.count(maxone),type(maxone),)
  if (len(t)==3):
    return t
  else:
    return (None,None,None)
  
